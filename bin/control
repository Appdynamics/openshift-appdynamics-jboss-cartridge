#!/bin/bash -eu

source $OPENSHIFT_CARTRIDGE_SDK_BASH

function status {
  client_result "AppDynamics Java AppServerAgent is enabled."
}


case "$1" in
  status)               status ;;
  *)           exit 0
esac

exit 0
